// ce fichier a été créé pour gérer le contexte
//  utilisateur dans l'application React,
// ça veut dire partager les informations de
// l'utilisateur connecté à travers différents
//  composants sans avoir à passer les props manuellement à chaque niveau.

//import { createContext, useState } from "react";

//const UserContext = createContext();

//export function UserProvider({ children }) {
  //const [user, setUser] = useState(null);

  //return (
    //<UserContext.Provider value={{ user, setUser }}>
      //{children}
    //</UserContext.Provider>
  //);
//}

//export default UserContext;
